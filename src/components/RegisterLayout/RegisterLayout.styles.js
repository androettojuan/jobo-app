import { styled } from "../../theme/styled";

export const StyledRegisterLayout = styled("div", {
    display: "flex",
    flexDirection: "column",
    margin: "0 16px",
})

export const StyledRegisterInputs = styled("div", {
    marginTop: "32px",
    display: "flex",
    flexDirection: "column",
    gap: "16px",
})