import {
  StyledRegisterInputs,
  StyledRegisterLayout,
} from "./RegisterLayout.styles";

const RegisterLayout = StyledRegisterLayout;

RegisterLayout.Inputs = StyledRegisterInputs;

export default RegisterLayout;
