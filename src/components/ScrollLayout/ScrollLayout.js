import { FixedPart, FullPage, ScrollPart } from "./ScrollLayout.styles";

const ScrollLayout = FullPage;

ScrollLayout.FixedPart = FixedPart;

ScrollLayout.ScrollPart = ScrollPart;

export default ScrollLayout;
