import React from "react";
import { StyledDescriptionAdmin } from "./DescriptionAdmin.styles";

const DescriptionAdmin = ({ children }) => {
  return <StyledDescriptionAdmin>{children}</StyledDescriptionAdmin>;
};

export default DescriptionAdmin;
