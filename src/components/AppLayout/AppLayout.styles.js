import { styled } from "../../theme/styled";

export const StyledFullApp = styled("div", {
  display: "flex",
  flexDirection: "column",
  height: "100%",
});

export const StyledAppContent = styled("div", {
  height: "100%",
  overflow: "hidden",
});

export const StyledBottomBarContainer = styled("div", {
  flexGrow: 0,
  flexShrink: 0,
  flexBasis: "auto",
  
});
